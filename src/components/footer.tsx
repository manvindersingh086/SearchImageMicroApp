import React from "react";
import classes from "../components/footer.module.css";

const footer = (props : any) => {
    return (
        <>
            <h1 className={classes.htag}></h1>
        </>
    )
}

export default footer;